<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cadastro</title>
    <link rel="stylesheet" href="gpt.css">
</head>

<body>
        <table>
            <thead>
                <tr>
                    <th scope="col">Nome:</th>
                    <th scope="col">Telefone:</th>
                    <th scope="col">Local:</th>
                    <th scope="col">Data:</th>
                </tr>
            </thead>
            <tbody>
                <% for (let i = 0; i < vetorVisitas.length; i++) { %>
                    <tr>
                        <td><%=vetorVisitas[i].nome%></td>
                        <td><%=vetorVisitas[i].telefone%></td>
                        <td><%=vetorVisitas[i].local%></td>
                        <td><%=vetorVisitas[i].dia%></td>
                    </tr>
                <% } %>
            </tbody>
        </table>
</body>
</html>
